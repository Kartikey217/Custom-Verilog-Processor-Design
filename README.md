# Custom-Verilog-Processor-Design
The processor supports a custom instruction format with arithmetic, logical, memory, and control-flow operations. It includes a register file, instruction register (IR), program counter (PC), instruction memory, and data memory, along with condition flags such as zero, carry, sign, and overflow.
